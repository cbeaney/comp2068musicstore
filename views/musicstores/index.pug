extends ../layouts/main.pug

block content
  .container
    header
      h1= title

    div
      table.table.table-striped
        thead
          tr
            th Title
            th Genre
            th Album
            th Artist
            th Price
        
        tbody
          each musicstore in musicstores
            tr
              td
                a(href=`/musicstores/${musicstore.id}`)= musicstore.title
              td= musicstore.status
              td
                a(href=`/musicstores/${musicstore.id}`)= musicstore.genre
              td= musicstore.status
              td
                a(href=`/musicstores/${musicstore.id}`)= musicstore.album
              td= musicstore.status
              td
                a(href=`/musicstores/${musicstore.id}`)= musicstore.artist
              td= musicstore.status
              td
                a(href=`/musicstores/${musicstore.id}`)= musicstore.price
              td= musicstore.status
              
                a(href=`/musicstores/${musicstore.id}/edit`) edit
                = ' | '
                form.destory(action=`/musicstores/destory`, method="POST", onsubmit=`return confirm("Are you sure you want to delete?")`)
                  input(type="hidden", name="id", value=musicstore.id)
                  input(type="submit", value="delete")
